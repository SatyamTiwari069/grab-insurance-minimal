# GrabInsurance Environment Variables (Optional)

# Port for HTTP API server (default: 8000)
PORT=8000

# For future: Claude API key if using live Claude integration
CLAUDE_API_KEY=your_key_here

# Node environment
NODE_ENV=development
